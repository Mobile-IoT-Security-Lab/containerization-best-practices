## -- Best practice 25: Dockerfile example (using dind) -- ##
FROM docker:20.10-dind

# Expose Docker daemon with TLS (Example, requires additional setup)
# CMD ["dockerd", "--host=tcp://0.0.0.0:2376", "--tlsverify", "--tlscacert=/certs/ca.pem", "--tlscert=/certs/server-cert.pem", "--tlskey=/certs/server-key.pem"]

# Start Docker-in-Docker daemon (secure version commented out for example)
CMD ["dockerd"]

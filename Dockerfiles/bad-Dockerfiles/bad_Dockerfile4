# Use a basic Python image
FROM python:3.9

# Set the working directory
WORKDIR /usr/src/app

### --- Use ADD to extract a local file ---###
ADD file.tar.gz .
###----------------------------------------###

#Copy the rest of the application code into the container
COPY . .

# Specifica il comando per avviare l'applicazione
CMD ["python", "app.py"]

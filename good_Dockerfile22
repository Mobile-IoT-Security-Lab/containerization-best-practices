# Good Example Dockerfile

FROM nginx:alpine

# Copy configuration and content
COPY nginx.conf /etc/nginx/nginx.conf
COPY index.html /usr/share/nginx/html/index.html

## -- Best practice 22: set the root filesystem to be immutable and read-only (ro) -- ##
CMD ["sh", "-c", "mount -o remount,ro / && nginx -g 'daemon off;'"]
## ---------------------------------------------------------------------------------- ##

version: '3.8'

services:
  app:
    build:
      context: .
    ports:
      - "80:80"
    environment:
      - DOCKER_OPTS="--log-level debug"
    volumes:
      - .:/app
    command: ["python", "app.py"]